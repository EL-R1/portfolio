{
  "name": "portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/generate-cv-pdf.mjs && node scripts/generate-cv-docx.mjs",
    "build:private": "node scripts/generate-cv-prive.mjs",
    "preview": "vite preview",
    "sync:projects": "node scripts/sync-github-projects.mjs",
    "prebuild": "node scripts/sync-github-projects.mjs",
    "deploy": "npm run build && gh-pages -d dist",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "typecheck": "vue-tsc --noEmit"
  },
  "dependencies": {
    "docx": "^9.7.1",
    "markdown-it": "^15.0.0",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "@vue/tsconfig": "^0.5.1",
    "happy-dom": "^20.9.0",
    "jsdom": "^27.0.1",
    "puppeteer-core": "^25.8.0",
    "typescript": "~5.4.0",
    "vite": "^5.0.0",
    "vitest": "^1.4.0",
    "vue-tsc": "^2.0.0"
  }
}
